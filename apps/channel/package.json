{
  "name": "@gpd/channel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GPD Telegram bot agent.",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx --watch src/server.ts",
    "start": "node --env-file=../../.env --import tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.36",
    "@gpd/api-client": "workspace:*",
    "@gpd/config": "workspace:*",
    "@gpd/contracts": "workspace:*",
    "ai": "^7.0.97",
    "grammy": "^1.46.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.7"
  }
}
