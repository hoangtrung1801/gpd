{
  "name": "@gpd/channel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GPD Slack thread agent via CopilotKit Channels.",
  "scripts": {
    "dev": "node --env-file=../../.env --import tsx --watch src/server.ts",
    "start": "node --env-file=../../.env --import tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@copilotkit/channels": "0.9.2",
    "@copilotkit/runtime": "1.70.3",
    "@ag-ui/client": "0.0.59",
    "@ag-ui/core": "0.0.59",
    "@ai-sdk/openai": "^3.0.36",
    "@gpd/api-client": "workspace:*",
    "@gpd/config": "workspace:*",
    "@gpd/contracts": "workspace:*",
    "rxjs": "7.8.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.7"
  }
}
